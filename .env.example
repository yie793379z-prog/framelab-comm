# Default behavior stays in mock mode.
AI_PROVIDER=mock
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# Legacy compatibility: AI_SUGGESTION_MODE=real falls back to OpenAI when AI_PROVIDER is unset.
# AI_SUGGESTION_MODE=mock
